# =============================================================================
# LLM AGENT CONFIGURATION — 5 Model Infrastructure
# =============================================================================
# Each LLM agent has its own:
# - Google Tag (GTAG) for analytics isolation
# - S3 Bucket for artifacts/logs
# - Pinecone Vector DB namespace for embeddings
# - Unique identifier

# =============================================================================
# GLOBAL CONFIGURATION
# =============================================================================

# Google Analytics API Key (shared)
GA_API_KEY=${GA_API_KEY}

# Pinecone Vector Database (shared instance, separate namespaces per LLM)
PINECONE_API_KEY=${PINECONE_API_KEY}
PINECONE_ENVIRONMENT=us-east-1
PINECONE_INDEX_NAME=origin-os-lam

# AWS Configuration (shared credentials, separate buckets)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}

# LLM Provider Keys
VERCEL_API_KEY=${VERCEL_API_KEY}
OPENROUTER_API_KEY=${OPENROUTER_API_KEY}

# =============================================================================
# LLM 1: CLAUDE (Anthropic)
# =============================================================================
CLAUDE_MODEL_ID=anthropic/claude-sonnet-4-20250514
CLAUDE_GTAG_ID=GT-CLAUDE001
CLAUDE_S3_BUCKET=origin-os-lam-claude
CLAUDE_VECTOR_NAMESPACE=claude
CLAUDE_VECTOR_DB_URL=https://origin-os-lam-${PINECONE_PROJECT_ID}.svc.${PINECONE_ENVIRONMENT}.pinecone.io

# =============================================================================
# LLM 2: GPT-4 (OpenAI)
# =============================================================================
GPT4_MODEL_ID=openai/gpt-4o
GPT4_GTAG_ID=GT-GPT4O002
GPT4_S3_BUCKET=origin-os-lam-gpt4
GPT4_VECTOR_NAMESPACE=gpt4
GPT4_VECTOR_DB_URL=https://origin-os-lam-${PINECONE_PROJECT_ID}.svc.${PINECONE_ENVIRONMENT}.pinecone.io

# =============================================================================
# LLM 3: GEMINI (Google)
# =============================================================================
GEMINI_MODEL_ID=google/gemini-2.0-flash
GEMINI_GTAG_ID=GT-GEMINI03
GEMINI_S3_BUCKET=origin-os-lam-gemini
GEMINI_VECTOR_NAMESPACE=gemini
GEMINI_VECTOR_DB_URL=https://origin-os-lam-${PINECONE_PROJECT_ID}.svc.${PINECONE_ENVIRONMENT}.pinecone.io

# =============================================================================
# LLM 4: LLAMA (Meta)
# =============================================================================
LLAMA_MODEL_ID=meta/llama-3.1-405b-instruct
LLAMA_GTAG_ID=GT-LLAMA004
LLAMA_S3_BUCKET=origin-os-lam-llama
LLAMA_VECTOR_NAMESPACE=llama
LLAMA_VECTOR_DB_URL=https://origin-os-lam-${PINECONE_PROJECT_ID}.svc.${PINECONE_ENVIRONMENT}.pinecone.io

# =============================================================================
# LLM 5: MISTRAL (Mistral AI)
# =============================================================================
MISTRAL_MODEL_ID=mistralai/mistral-large
MISTRAL_GTAG_ID=GT-MISTRAL5
MISTRAL_S3_BUCKET=origin-os-lam-mistral
MISTRAL_VECTOR_NAMESPACE=mistral
MISTRAL_VECTOR_DB_URL=https://origin-os-lam-${PINECONE_PROJECT_ID}.svc.${PINECONE_ENVIRONMENT}.pinecone.io
